import React from 'react';

const FlameTrailStreak = ({ streak }) => (
  <div className="text-sm text-red-500 font-bold">
    🔥 Ritual Streak: {streak} days
  </div>
);

export default FlameTrailStreak;
